--- origsrc/clisp-2.49/src/clisp-link.in	2010-05-25 15:54:54.000000000 -0400
+++ src/clisp-2.49/src/clisp-link.in	2015-02-20 11:54:55.891177200 -0500
@@ -26,9 +26,13 @@ normalize(){                    # dir re
 lncp () {
   # Make a link from $1 to $2. Try symbolic link, hard link, file copying.
   rm -f "$2"
-  ln -s "`normalize . $1`" "$2" 2>/dev/null || \
-    ln "$1" "$2" 2>/dev/null || \
+  if test ${1/.dll} != $1; then 
     cp -p "$1" "$2"
+  else
+    ln -s "`normalize . $1`" "$2" 2>/dev/null || \
+      ln "$1" "$2" 2>/dev/null || \
+      cp -p "$1" "$2"
+  fi
 }
 
 lncp_some () {
